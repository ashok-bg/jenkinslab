node {
  checkout scm
    stage('build') {
      echo 'build'
    }
  stage('test') {
    echo 'testing'
  }
   stage('deploy') {
    echo 'deploying'
  }
   stage('validate') {
    echo 'validating'
  }
}
    
